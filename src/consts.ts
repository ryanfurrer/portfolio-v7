/**
 * Site-wide constants — the single source of truth for global metadata
 * defaults. Pages still override per-page by passing `title` / `description`
 * to <Layout> (which forwards them to <Head>); these are only the fallbacks.
 */

export const SITE_NAME = "Ryan Furrer";

export const SITE_TITLE = `${SITE_NAME} — Frontend Engineer and Designer`;

export const SITE_DESCRIPTION =
  "I am a Frontend Engineer who feels at home where design and engineering meet. Crafting interfaces down to the last detail.";
