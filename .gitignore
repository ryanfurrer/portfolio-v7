# build output
dist/
# generated types
.astro/
# Sanity TypeGen intermediate schema (regenerated by `pnpm typegen`;
# the generated src/sanity/sanity.types.ts is committed)
schema.json

# Sanity Studio runtime (regenerated whenever the studio/dev server runs)
.sanity/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Vercel
.vercel/

# Claude Code — local agent/editor tooling (launch config, local settings).
# Blanket-ignored for this solo repo. If you later add shareable config
# (.claude/commands, .claude/agents, a committed settings.json), un-ignore it
# with a negation line below, e.g. `!.claude/commands/`.
.claude/